'use strict';

// Reuse functions wherever possible.

// 1. Write a function that takes a string argument
// splits it into an array of normalized words
// (uppercase strings without punctuation)
// and returns that array.
const normalizeWords = function (a,b,c,d,e) {
  normalizeWords=("boat", "dock", "water", "sail", "cleat")
};
return normalizeWords;


// 2. Write a function that takes a string argument
// and returns an array of unique normalized words.
const uniqueWords = function (a,b,c,d,e) {
  uniqueNormalizedWorlds=("Gracie", "Block", "sound", "jib")
};
console.log(0,1,2,3,4)

// 3. Write a function that takes a string argument and returns the count of
// words in a string.
// Provide the *option* to count unique words instead of total words, by passing
// a second argument.

const wordCount = function (/* */) {
  /* your response here */
};
I am lost again.I am just understanding definitions. I guess. Object, variables, values but still very unsure of myself and certainly functions. I know what functions are, just not how to code them
// 4. Write a function that takes a string and returns a dictionary with unique
// words as keys and a count of each word as the value for that key
const wordFrequencies = function (/* */) {
  /* your response here */
};

module.exports = {
  normalizeWords,
  uniqueWords,
  wordCount,
  wordFrequencies,
};
