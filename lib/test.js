let upper = '' // attempted placing it here so it would return//
const normalizeWords = function (words) {
  let upper = words.toUpperCase()
  console.log('upper') // can't get this to return in terminal
  return upper // can't get this to return in terminal when trying to test
}
console.log(upper)
